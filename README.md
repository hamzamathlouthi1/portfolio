# Portfolio

Single-page Angular 19 portfolio for Hamza Mathlouthi.

```bash
npm install
npm start        # http://localhost:4200
npm run build    # output: dist/portfolio/browser
```

Deploy on Vercel: import the GitHub repo. `vercel.json` already sets the build command and output directory.
